STEP 4:
Create a .env file to store sensitive environment variables
 Add the following variables to it:
 PORT=3001
 DATABASE_USER=postgres
 DATABASE_PASS=postgres
 DATABASE_HOST=localhost
 DATABASE_PORT=5432
 DATABASE_NAME=vaccine_hub
Create a config.js file at the root of the project.
 Extract all needed info from process.env into local variables
 Log your configuration to the terminal
 Export all the necessary config variables
Import the PORT variable from config into server.js and use it when the app starts up.